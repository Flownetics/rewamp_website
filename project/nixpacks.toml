[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = [
  'npm install',
  'cd server && npm install && cd ..'
]

[phases.build]
cmds = [
  'npm run build'
]

[start]
cmd = 'cd server && node index.js'

